apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bins.fullname" . }}-test-connection"
  labels:
    {{- include "bins.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bins.fullname" . }}-graphql:{{ .Values.service.port }}']
  restartPolicy: Never
